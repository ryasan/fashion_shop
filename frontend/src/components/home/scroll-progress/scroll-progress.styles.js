import styled from 'styled-components'

const ScrollProgress = styled.div`
  color: var(--red);
  font-size: 3rem;
  position: fixed;
  right: 2rem;
  top: 30%;
  z-index: 9999;
`

export default ScrollProgress
